#ifndef IND_FUNC
#define IND_FUNC

#include <string>
#include <sstream>

using namespace std;

string addIntToString(string text, int number) {
	stringstream stream;
	stream << id;
	text += stream.str();
	return text;
}

#endif